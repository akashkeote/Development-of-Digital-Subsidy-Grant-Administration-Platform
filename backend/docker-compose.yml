version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: subsidy_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: subsidy_db
    ports:
      - "3306:3306"
